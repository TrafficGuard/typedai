:host {
    display: block;
    height: 100%;
    width: 100%;
}

.code-edit-container {
    height: 100%;
    width: 100%;
}

.panel-content {
    padding: 1rem;
    height: 100%;
    display: flex;
    flex-direction: column;
    overflow: auto;
}

.placeholder {
    flex-grow: 1;
    border: 1px dashed var(--fuse-border);
    border-radius: 4px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--fuse-text-secondary);
}
.code-edit-container {
	@apply h-full w-full;
}

.panel-content {
	@apply h-full w-full p-4 overflow-auto;
	@apply bg-card;
}

.file-tree {
	.mat-tree-node {
		@apply min-h-8;
	}

	.node-name {
		@apply ml-2;
	}

	.leaf-node {
		@apply pl-10; // 40px, same as mat-icon-button width
	}

	.mat-tree-node-toggle-placeholder {
		@apply w-10; // 40px
	}

	.tree-node-container-invisible {
		@apply hidden;
	}
}
// Basic styling for the file tree to ensure it's readable.
.file-tree {
    background-color: transparent; // Match panel background

    .mat-tree-node,
    .mat-nested-tree-node {
        min-height: 32px;
        display: flex;
        align-items: center;
        cursor: pointer;

        &:hover {
            background-color: rgba(255, 255, 255, 0.05);
        }
    }

    .node-name {
        margin-left: 8px;
    }

    .mat-icon {
        vertical-align: middle;
    }

    .leaf-node {
        // Indent leaf nodes to align with folders.
        .mat-tree-node-toggle-placeholder {
            width: 40px; // Same width as the expand/collapse button.
        }
    }

    // Make the nested div take up the full width
    .tree-node-container-invisible {
        display: none;
    }
}
:host {
	display: block;
	height: 100%;
}

.code-edit-container {
	height: 100%;
}

.panel-content {
	padding: 1rem;
	height: 100%;
	display: flex;
	flex-direction: column;
	overflow: hidden; // Prevent padding from causing scrollbars
}

.file-tree {
	flex-grow: 1;
	overflow: auto;
	background: transparent;
}

.mat-tree-node {
	display: flex;
	align-items: center;
	min-height: 40px;
	padding-right: 8px;
	border-radius: 4px;
	cursor: pointer;

	&.leaf-node {
		font-weight: normal;
	}

	&.selected {
		background-color: var(--fuse-primary-50, rgba(25, 118, 210, 0.08));
	}

	.mat-icon {
		margin-right: 8px;
	}

	.node-checkbox {
		margin-right: 4px;
	}

	.node-name {
		flex-grow: 1;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
}

.mat-tree-node-toggle-placeholder {
	width: 40px; // same width as the icon button
}

.tree-node-container-invisible {
	display: none;
}

.table-container {
	flex-grow: 1;
	overflow: auto;
	border: 1px solid var(--fuse-border);
	border-radius: 6px;
}

.selection-table {
	width: 100%;

	.mat-mdc-header-cell {
		font-weight: 600;
	}

	.mat-mdc-cell {
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
		padding-right: 16px;
	}

	.mat-column-actions {
		width: 48px; // For the icon button
		text-align: right;
		padding: 0 8px 0 0;
	}
}
:host {
	display: block;
	height: 100%;
}

.code-edit-container {
	height: 100%;
}

.panel-content {
	padding: 1rem;
	height: 100%;
	display: flex;
	flex-direction: column;
	overflow: hidden; // Prevent padding from causing scrollbars
}

.file-tree {
	flex-grow: 1;
	overflow: auto;
	background: transparent;
}

.mat-tree-node {
	display: flex;
	align-items: center;
	min-height: 40px;
	padding-right: 8px;
	border-radius: 4px;
	cursor: pointer;

	&.leaf-node {
		font-weight: normal;
	}

	&.selected {
		background-color: var(--fuse-primary-50, rgba(25, 118, 210, 0.08));
	}

	.mat-icon {
		margin-right: 8px;
	}

	.node-checkbox {
		margin-right: 4px;
	}

	.node-name {
		flex-grow: 1;
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
}

.mat-tree-node-toggle-placeholder {
	width: 40px; // same width as the icon button
}

.tree-node-container-invisible {
	display: none;
}

.table-container {
	flex-grow: 1;
	overflow: auto;
	border: 1px solid var(--fuse-border);
	border-radius: 6px;
}

.selection-table {
	width: 100%;

	.mat-mdc-header-cell {
		font-weight: 600;
	}

	.mat-mdc-cell {
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
		padding-right: 16px;
	}

	.mat-column-actions {
		width: 48px; // For the icon button
		text-align: right;
		padding: 0 8px 0 0;
	}
}
:host {
	display: block;
	height: 100%;
}

.code-edit-container {
	height: 100%;
	width: 100%;
}

.panel-content {
	padding: 1rem;
	height: 100%;
	display: flex;
	flex-direction: column;
	overflow: hidden; // Hide overflow, child elements will manage their own scrolling
	box-sizing: border-box;

	.file-tree,
	.table-container {
		flex-grow: 1;
		overflow-y: auto;
	}
}

.table-container {
	.selection-table {
		width: 100%;
	}

	.mat-column-actions {
		width: 48px;
		text-align: right;
	}
}

form {
	flex-grow: 1;

	textarea {
		resize: none;
		min-height: 100px;
	}
}

// Style for selected tree nodes to provide visual feedback
.mat-tree-node.selected,
.mat-tree-node.leaf-node.selected {
	background-color: rgba(0, 123, 255, 0.08);
}

.node-name {
	margin-left: 8px;
	white-space: nowrap;
	overflow: hidden;
	text-overflow: ellipsis;
}

.tree-node-container-invisible {
    display: none;
}

// Optional: Style the split gutter for a more subtle look
::ng-deep {
	as-split-gutter {
		background-color: #f0f0f0;

		&:hover {
			background-color: #e0e0e0;
		}
	}
}
// Basic styling for the file tree to ensure it's readable.
.file-tree {
	background-color: transparent; // Match panel background

	.mat-tree-node,
	.mat-nested-tree-node {
		min-height: 32px;
		display: flex;
		align-items: center;
		cursor: pointer;

		&:hover {
			background-color: rgba(255, 255, 255, 0.05);
		}
	}

	.node-name {
		margin-left: 8px;
	}

	.mat-icon {
		vertical-align: middle;
	}

	.leaf-node {
		// Indent leaf nodes to align with folders.
		.mat-tree-node-toggle-placeholder {
			width: 40px; // Same width as the expand/collapse button.
		}
	}

	// Make the nested div take up the full width
	.tree-node-container-invisible {
		display: none;
	}
}
