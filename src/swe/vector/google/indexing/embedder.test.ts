import { expect } from 'chai';
import sinon from 'sinon';

describe('Embedder', () => {
	describe.skip('generateEmbedding (with VertexAI integration)', () => {
		it('should call Vertex AI with correct task type for RETRIEVAL_DOCUMENT', async () => {
			// Test logic here
		});
		it('should call Vertex AI with correct task type for CODE_RETRIEVAL_QUERY', async () => {
			// Test logic here
		});
	});
});
