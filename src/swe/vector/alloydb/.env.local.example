# AlloyDB Omni Local Development Configuration
# Copy this file to .env.local and adjust values as needed

# === Database Connection ===
ALLOYDB_HOST=localhost
ALLOYDB_PORT=5432
ALLOYDB_DATABASE=vector_db
ALLOYDB_USER=postgres
ALLOYDB_PASSWORD=alloydb123

# === AlloyDB Features ===
# Embedding model for automated embeddings
ALLOYDB_EMBEDDING_MODEL=gemini-embedding-001

# Enable columnar engine (may not be available in AlloyDB Omni)
ALLOYDB_ENABLE_COLUMNAR_ENGINE=false

# Hybrid search vector weight (0-1, text weight = 1 - vector_weight)
ALLOYDB_VECTOR_WEIGHT=0.7

# === Google Cloud (for Vertex AI embeddings and reranking) ===
# Required for:
# - Dual embedding (code embeddings)
# - Reranking
# - Generating query embeddings (if automated embeddings don't work in query)
GCLOUD_PROJECT=your-gcp-project
GCLOUD_REGION=us-central1

# === Optional: Alternative Connection String ===
# If you prefer to use a connection string instead of individual params
# ALLOYDB_CONNECTION_STRING=postgresql://postgres:alloydb123@localhost:5432/vector_db

# === Vector Search Configuration ===
# These can be overridden per-repository via .vectorconfig.json
VECTOR_CONTEXTUAL_CHUNKING=true
VECTOR_DUAL_EMBEDDING=false
VECTOR_HYBRID_SEARCH=true
VECTOR_RERANKING=false
VECTOR_CHUNK_SIZE=2500
VECTOR_CHUNK_OVERLAP=300
